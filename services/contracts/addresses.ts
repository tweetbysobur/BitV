import type { DeployedContract } from "./types";

/**
 * Deployed BitV contract addresses on Monad Testnet.
 * Empty until contracts are designed, written, and deployed — do not
 * populate with placeholder/fake addresses.
 */
export const contractAddresses: Record<string, DeployedContract> = {};
